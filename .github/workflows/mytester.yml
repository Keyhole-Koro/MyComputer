name: CI - MyTester

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  mytester-smoke:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Rust
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential g++

      - name: Verify Python scripts
        run: |
          python3 -m py_compile \
            tools/project_paths.py \
            qa/as-test.py \
            qa/build_toolchain.py \
            qa/linker-test.py \
            qa/mlc-test.py \
            qa/run_kernel.py \
            qa/serial-rx-test.py

      - name: Run MyTester smoke tests
        run: |
          python3 qa/as-test.py simpleChar
          python3 qa/mlc-test.py simpleFunc
          python3 qa/run_kernel.py --no-run
          python3 qa/serial-rx-test.py

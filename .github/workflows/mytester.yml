name: CI - MyTester

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  mytester-smoke:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          submodules: recursive

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install Rust
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential g++

      - name: Verify Python scripts
        run: |
          python3 -m py_compile \
            tools/project_paths.py \
            qa/as-test.py \
            qa/build_toolchain.py \
            qa/linker-test.py \
            qa/mlc-test.py \
            qa/run_kernel.py \
            qa/serial-rx-test.py

      - name: Run MyTester smoke tests
        run: |
          make as-test ARGS="simpleChar"
          make mlc-test ARGS="simpleFunc"
          make kernel-build
          make serial-rx-test
